// The file contents for the current environment will overwrite these during
// build. The build system defaults to `environment.ts`, but if you do
// `ng build --env=dev/prod` then `environment.dev|prod.ts` will be used
// instead. The list of which env maps to which file can be found
// in `.angular-cli.json`.

export const environment = {
  production: false,
  fbAppId: '948235391925373'
};
